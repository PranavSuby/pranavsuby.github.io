export const chartTooltip = {
  contentStyle: {
    background: 'var(--ui-surface2)',
    border: '1px solid var(--ui-border)',
    borderRadius: 8,
    fontSize: 12,
    color: 'var(--ui-text)',
  },
  labelStyle: { color: 'var(--ui-text2)' },
};

export const chartAxis = {
  tick: { fontSize: 10, fill: 'var(--ui-text3)' },
  axisLine: false,
  tickLine: false,
};
